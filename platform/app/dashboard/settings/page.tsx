export default function DashboardSettingsPage() {
  return (
    <section className="panel">
      <p className="eyebrow">Settings</p>
      <h1 className="pageTitle">Site and organization settings</h1>
      <p className="muted">
        In the migration, this route will manage branding, page modules, authentication rules, public navigation, and
        record schema configuration.
      </p>
    </section>
  );
}
