export default function DashboardMediaPage() {
  return (
    <section className="panel">
      <p className="eyebrow">Media</p>
      <h1 className="pageTitle">Media pipeline placeholder</h1>
      <p className="muted">
        This module is where the migration will handle original files, web derivatives, thumbnails, captions, and primary
        image selection.
      </p>
    </section>
  );
}
